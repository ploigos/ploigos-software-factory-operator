{% for user in openshift_users %}
- apiVersion: user.openshift.io/v1
  kind: User
  identities:
  - '{{ htpasswd_provider_name }}:{{ user.username }}'
  metadata:
    name: '{{ user.username }}'
{% endfor %}

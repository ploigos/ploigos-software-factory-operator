
# OperatorGroup for OLM configuration
- apiVersion: operators.coreos.com/v1
  kind: OperatorGroup
  metadata:
    name: {{ operator_group_name | default(selenium_grid_project_name + '-operatorgroup') }}
    namespace: {{ selenium_grid_project_name }}
  spec:
    targetNamespaces:
    - {{ selenium_grid_project_name }}

# Subscription to trigger OLM installation
- apiVersion: operators.coreos.com/v1alpha1
  kind: Subscription
  metadata:
    name: selenium-grid-operator
    namespace: {{ selenium_grid_project_name }}
  spec:
    channel: alpha
    installPlanApproval: Automatic
    name: selenium-grid-operator
    source: redhatgov-operators
    sourceNamespace: openshift-marketplace
